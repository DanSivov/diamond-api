[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --max-requests 100"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
